# Description: Simple crond
# URL:         https://github.com/zeppe-lin/crond

name=crond
version=0.1-a25abb5
release=1
source=(https://github.com/zeppe-lin/$name/archive/${version##*-}/$name-${version##*-}.tar.gz
	crontab crond.rc runjobs)

build() {
	cd $name-${version##*-}*

	make DESTDIR=$PKG PREFIX=/usr install

	install -m 600 -D $SRC/crontab  $PKG/etc/crontab
	install -m 755 -D $SRC/crond.rc $PKG/etc/rc.d/crond
	install -m 755 -D $SRC/runjobs  $PKG/usr/bin/runjobs

	install -d $PKG/etc/cron/{hourly,daily,weekly,monthly}
}
